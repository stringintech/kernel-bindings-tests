{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "./shared.json#/$defs/RefResponse",
  "description": "Contains the returned transaction input (e.g., `{\"ref\": \"$transaction_input\"}`). The returned transaction input reference is a view that depends on the lifetime of the parent transaction and does not require an explicit destroy call."
}
