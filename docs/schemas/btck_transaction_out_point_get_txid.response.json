{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "./shared.json#/$defs/RefResponse",
  "description": "Contains the returned txid (e.g., `{\"ref\": \"$txid\"}`). The returned txid reference is a view that depends on the lifetime of the parent transaction out point and does not require an explicit destroy call."
}
